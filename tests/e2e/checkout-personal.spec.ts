import test from "@playwright/test";

// TODO
test.describe.skip('personal checkout validation tests', {tag: ['@checkout-personal-validation']},() => {
  
  test.beforeEach(async ({ page }) => {
  });

  test('fill wrong data', async ({page}) => {
  
  });

  test('miss some other', async ({page})=> {
  
  });

});
