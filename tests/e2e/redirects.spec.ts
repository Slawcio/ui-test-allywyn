import test from "@playwright/test";

// TODO
test.describe('redirects', {tag: ['@redirects']},() => {
  
  test.beforeEach(async ({ page }) => {
  });

  test('redirects burger menu', async ({page}) => {
  
  });

  test('redirects from checkout pages', async ({page})=> {
  
  });

});
